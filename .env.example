# Server
PORT=3000
SESSION_SECRET=change_me

# Database
DB_PATH=./data/app.db

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@example.com
SMTP_PASS=your_app_password
MAIL_FROM=Accounting Reminders <your_email@example.com>

# Reminder behavior
# Comma-separated days-before values. Example: 7,3,1,0 (0 = due date)
REMINDER_DAYS_BEFORE=7,3,1,0

# Where reminder links point (optional)
APP_BASE_URL=http://localhost:3000
